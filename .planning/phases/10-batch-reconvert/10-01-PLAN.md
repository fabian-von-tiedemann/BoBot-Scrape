---
phase: 10-batch-reconvert
plan: 01
type: execute
depends_on: []
files_modified: []
---

<objective>
Re-convert all existing documents to apply Phase 9 improvements (URL-decoded filenames, source_url field).

Purpose: Apply output quality improvements from Phase 9 to all 1143 existing converted files.
Output: All markdown files in converted/ updated with readable Swedish filenames, decoded titles, and source_url fields.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-output-quality/09-01-SUMMARY.md
@convert.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear existing converted files and re-run conversion</name>
  <files>converted/</files>
  <action>
    1. Remove all existing .md files in converted/ directory (they have URL-encoded names and lack source_url)
    2. Run convert.py --force --skip-ai to regenerate all files with improvements from Phase 9
    3. Using --skip-ai because AI metadata isn't the focus here - just filename/URL improvements
  </action>
  <verify>
    - ls converted/ shows category folders with readable Swedish names in filenames
    - Sample file frontmatter contains decoded title and source_url field
    - Count of converted files matches expected (~1143)
  </verify>
  <done>All files re-converted with URL-decoded filenames and source_url in frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Verify output quality improvements</name>
  <files>converted/</files>
  <action>
    Spot-check several converted files to confirm:
    1. Filenames are readable Swedish (Ändring, Anmäla, Återkallelse, etc.)
    2. Frontmatter title field is decoded Swedish text
    3. source_url field is present and contains valid URL
    4. source_file path is unchanged (still URL-encoded for disk traceability)
  </action>
  <verify>
    - head -10 on sample files shows decoded title and source_url
    - ls shows readable filenames without %XX encoding
  </verify>
  <done>Output quality matches Phase 9 requirements - readable filenames, source_url present</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] converted/ directory contains ~1143 markdown files
- [ ] Filenames are URL-decoded (readable Swedish characters)
- [ ] Frontmatter title is URL-decoded
- [ ] Frontmatter source_url field present
- [ ] Frontmatter source_file remains URL-encoded (for traceability)
</verification>

<success_criteria>

- All files re-converted
- URL-decoded filenames throughout converted/
- source_url field in all frontmatter
- Phase 10 complete, v2.1 milestone shipped
</success_criteria>

<output>
After completion, create `.planning/phases/10-batch-reconvert/10-01-SUMMARY.md`
</output>
